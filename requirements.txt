fastapi>=0.110.0
uvicorn[standard]>=0.27.0
apscheduler>=3.10.4
httpx>=0.27.0
python-multipart>=0.0.9
psutil>=5.9.0
websockets>=12.0
cryptography>=42.0
PyJWT>=2.8      # verify Cloudflare Access JWTs at the origin (cf_access.py)
# The fleet's single SDK pin lives in foxxe-mcp (PRD G1). The [fastmcp]
# extra bounds BOTH names together, which is what rialu needs: unbounded,
# `fastmcp>=2.0.0` resolves to fastmcp 4.0.2 and pulls mcp 2.1.1
# transitively, where mcp.server.fastmcp is a stub that raises on import.
foxxe-mcp[fastmcp] @ git+https://github.com/todd427/foxxe-mcp@v0.1.0
